server:
  port: 80
  sources:
    - path: /documents
      name: Documentos
    - path: /photos
      name: Fotos
    - path: /movies
      name: Vídeos
      config:
        defaultEnabled: true
frontend:
  name: "FileBrowser"
auth:
  adminUsername: admin
  adminPassword: admin
  methods:
    password:
      enabled: true
integrations:
  media:
    ffmpegPath: "/usr/local/bin"
  office:
    url: "http://localhost:8080"   # http://IP:8080 ó https://tudominio.com
    secret: 066a60a2e469df3b6fa0b45e92c920062a01287f
